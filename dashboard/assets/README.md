# Dashboard assets

Drop a robot image here named `robot.png` (or .jpg/.webp) and the Portfolio cover
will render it on the right with the gradient fade. Without it, a dark-gradient
fallback is shown. The image is volume-mounted on the NAS — no rebuild needed;
just place it and refresh the dashboard.
